{
 "id": "e20_gate_untie",
 "note": "Do the two winners STACK? Gated attention (iso-param variant, inter 6192, -0.057) plus untied embeddings (-0.093). The mechanisms are unrelated -- one gates attention output, the other stops sharing the input embedding with the output head -- so additivity is the null hypothesis and roughly 6.053 is the prediction. Sub-additivity would mean they are partly solving the same problem, which is worth knowing before both go into the production config. Watch HBM: e19 alone hit 91.6%.",
 "hidden_size": 2560,
 "num_hidden_layers": 24,
 "num_attention_heads": 10,
 "num_key_value_heads": 2,
 "intermediate_size": 6192,
 "mlp_type": "swiglu",
 "qk_norm": true,
 "v_norm": true,
 "sandwich_norm": true,
 "logit_softcap": 15.0,
 "rope_theta": 1000000.0,
 "tie_word_embeddings": false,
 "interleaved_local_attention": true,
 "local_attention_window": 256,
 "block_size": 2048,
 "micro_batch": 8,
 "grad_accum": 5,
 "train_tokens": 35000000,
 "lr": 0.0003,
 "grad_clip": 0.4,
 "cooldown_frac": 0.15,
 "fp8": 1,
 "fp8_lm_head": 1,
 "torch_compile": 1,
 "compile_mode": "default",
 "grad_checkpointing": 1,
 "loss_chunk_size": 0,
 "seed": 444,
 "data_seed": 1337,
 "wall_guard_sec": 1620,
 "checkpoint_stride": 2,
 "eval_lengths": [
  2048,
  4096,
  8192
 ],
 "warmup": 14,
 "attn_gate": true
}